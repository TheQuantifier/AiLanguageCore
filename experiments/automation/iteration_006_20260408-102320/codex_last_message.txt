I removed the default `benchmark_stress.json` injection from the training conversion path, because the latest regression aligned with a new `TOOL_NEEDED` bias after that augmentation was introduced. The next run should train on the curated split by default again, while still allowing explicit extras later if needed.

I regenerated the SFT files so the repo is ready for the next training iteration now. The rebuilt train set is back to 556 examples with this label mix: `OUT_OF_SCOPE` 83, `CLARIFICATION` 95, `DIRECT_ANSWER` 156, `TOOL_NEEDED` 222.

Files changed:
- [scripts/convert_training_data.py](C:/Users/jhand/Documents/Github/AiLanguageCore/scripts/convert_training_data.py)
- [data/processed/train_sft.jsonl](C:/Users/jhand/Documents/Github/AiLanguageCore/data/processed/train_sft.jsonl)
- [data/processed/validation_sft.jsonl](C:/Users/jhand/Documents/Github/AiLanguageCore/data/processed/validation_sft.jsonl)

AUTOMATION_DECISION: CONTINUE